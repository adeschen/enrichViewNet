CHANGES IN VERSION 1.7.3
------------------------

NEW FEATURES

    o The new functions createBasicEmapAsIgraph(), createEnrichMapMultiBasicAsIgraph(), generates enrichment maps in an igraph format.


CHANGES IN VERSION 1.7.2
------------------------

BUG FIXES AND IMPROVEMENTS

    o All functions using ggplot2 are using updated ggplot2 functions.


CHANGES IN VERSION 1.7.1
-------------------------

BUG FIXES

    o The package uses a fixed version of gprofiler database in the unit tests.
  
  
CHANGES IN VERSION 1.3.4
-------------------------

NEW FEATURES

    o The 'force' parameter in createEnrichMapMultiComplex() has been removed.
    o The 'force' parameter in createEnrichMapMultiBasic() has been removed.
    o The 'force' parameter in createEnrichMap() has been removed.


CHANGES IN VERSION 1.3.3
-------------------------

NEW FEATURES

    o The createEnrichMapMultiComplex() now has an new parameter ... that enables passing extra parameters to imbedded emapplot() function.
    o The createEnrichMapMultiBasic() now has an new parameter ... that enables passing extra parameters to imbedded emapplot() function.
    o The createEnrichMap() now has an new parameter ... that enables passing extra parameters to imbedded emapplot() function.


CHANGES IN VERSION 1.3.2
-------------------------

BUG FIXES

    o The package uses the Ensembl list rather than the query list to extract information when intersection column is missing, in the extractInformationWhenNoIntersection() function.
    

CHANGES IN VERSION 1.3.1
-------------------------

BUG FIXES

    o The package uses the information about the organism when querying the gprofiler database.
    

CHANGES IN VERSION 1.1.2
-------------------------

NEW FEATURES

    o The new function createEnrichMapMultiBasic() enables the creation of enrichment maps with groups from simple designs.
    o The new function createEnrichMapMultiComplex() enables the creation of enrichment maps with groups from complex designs. 

SIGNIFICANT USER-VISIBLE CHANGE

    o The vignette contains new sections presenting the new functions createEnrichMapMultiBasic() and createEnrichMapMultiComplex().
    

CHANGES IN VERSION 1.1.1
-------------------------

SIGNIFICANT USER-VISIBLE CHANGE

    o The enrichViewNet workflow figure, in the vignette, has been updated.


CHANGES IN VERSION 0.99.2
-------------------------

NEW FEATURES

    o The man pages are respecting 80 character width.


CHANGES IN VERSION 0.99.1
-------------------------

NEW FEATURES

    o The 'Installation' and 'Introduction' sections of the vignette have been updated.


CHANGES IN VERSION 0.99.0
-------------------------

NEW FEATURES

    o The new createEnrichMap() function enables the creation of an enrichment map from enrichment results.


CHANGES IN VERSION 0.0.4
-------------------------

NEW FEATURES

    o When 'intersection' column present in the GOST object, the software uses the column to create the output for Cytoscape, without having to call gconvert() function. The output is generated much more rapidly.


CHANGES IN VERSION 0.0.2
-------------------------

NEW FEATURES

    o Added a NEWS.md file to track changes to the package.

SIGNIFICANT USER-VISIBLE CHANGES

    o The createNetwork() method has a new parameter 'fileName' that enables the creation of a CX JSON file when Cytoscape is not running.

BUG FIXES

    o The createCytoscapeCXJSON() method does not replicate anymore the genes that are associated to more than one term.


